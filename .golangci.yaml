run:
  timeout: 5m

linters-settings:
  gocritic:
    enabled-tags:
    - performance
  gosec:
    excludes:
    - G108
  lll:
    line-length: 200

  misspell:
    locale: US
  staticcheck:
    go: "1.17"

linters:
  disable-all: true
  enable:
    - deadcode
    - errcheck
    - ineffassign
    - gocritic
    - goconst
    - gofmt
    - goimports
    - gosec
    - gosimple
    - govet
    - misspell
    - revive
    - staticcheck
    - structcheck
    - typecheck
    - unused
    - varcheck
